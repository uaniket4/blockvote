# Copy this file to .env.sync and set values once.
# `npm run new-cycle:cloud` reads these variables automatically.

# Your deployed backend base URL (no trailing /api)
CONTRACT_SYNC_BACKEND_URL=https://blockvote-sw2r.onrender.com

# Must match CONTRACT_SYNC_SECRET in deployed backend environment
CONTRACT_SYNC_SECRET=replace-with-strong-secret
